# Ask
We have single large EMR cluster running on Kubernetes - this platform is managed by us and used by the modellers. We
use Cloudability from Aptio for chargeback.  
Our ask is to create chargeback by month by user from Jan 2025Jan thru 2026Feb. Namespaces are defined and most of these
match our users some of these names spaces refer to services- can u list these if you know.

Use curl command to pull cloudability data for given clusterid
To pull by each month in csv format

within code show output of each step

which is the right column to use for cost unblended or fairshare,
All the cost computed per month should add up to cloudability monthly code
Will we add service cost other paltform cost proportionately across all users
how do calculate change by month for each user. 
